import { createContext,useContext,useEffect,useState } from 'react'; import { Footer } from '../components/layout/Footer'; import { Header } from '../components/layout/Header'; import { OnboardingModal } from '../components/onboarding/OnboardingModal'; import { OnboardingProvider } from '../state/onboardingContext'; import type { Role } from '../types/onboarding'; import { FAQPage,HomePage,InfoPage,LegalPage,NotFoundPage } from '../routes/Pages';
type Navigate=(to:string)=>void; const NavContext=createContext<Navigate>(()=>{}); export function Link({to,children,...props}:{to:string;children:React.ReactNode}&React.AnchorHTMLAttributes<HTMLAnchorElement>){const nav=useContext(NavContext);return <a href={to} {...props} onClick={e=>{props.onClick?.(e);if(!e.defaultPrevented&&to.startsWith('/')){e.preventDefault();nav(to)}}}>{children}</a>}
export default function App(){const [path,setPath]=useState(location.pathname),[onboarding,setOnboarding]=useState<{open:boolean;role?:Role}>({open:false});useEffect(()=>{const h=()=>setPath(location.pathname);addEventListener('popstate',h);return()=>removeEventListener('popstate',h)},[]);const navigate=(to:string)=>{history.pushState({},'',to);setPath(to);scrollTo(0,0)};const open=(role?:Role)=>setOnboarding({open:true,role});let page:React.ReactNode;switch(path){case '/':page=<HomePage open={open}/>;break;case '/about':page=<InfoPage title="A better place for creative work." eyebrow="ABOUT CULTGIG" open={open}>Cultgig exists because creative work is too often discovered through a scattered web of DMs and referrals. We are building a more thoughtful way for artists and businesses to introduce themselves.</InfoPage>;break;case '/how-it-works':page=<InfoPage title="A clearer start for every collaboration." eyebrow="HOW IT WORKS" open={open}>Artists create a profile, show their work, and tell us where they are. Businesses share what they need. Discovery and booking are future product phases; today, Cultgig creates the foundation.</InfoPage>;break;case '/for-artists':page=<InfoPage title="Your work is your proof." eyebrow="FOR ARTISTS" open={open} role="artist">Build a profile that reflects your craft — from musicians and DJs to photographers, decorators, dancers, and performers. A profile is the first step toward being discovered.</InfoPage>;break;case '/for-businesses':page=<InfoPage title="Find the right creative fit." eyebrow="FOR BUSINESSES" open={open} role="client">Tell us about your business and creative needs. Profile-based discovery and booking are where Cultgig is headed; joining now helps shape that marketplace.</InfoPage>;break;case '/faq':page=<FAQPage/>;break;case '/contact':page=<InfoPage title="Let’s talk." eyebrow="CONTACT" open={open}>For general questions, artist support, or partnership conversations, contact [SUPPORT EMAIL]. Please replace this placeholder in <code>src/config/site.ts</code> before launch.</InfoPage>;break;case '/privacy':page=<LegalPage kind="Privacy Policy"/>;break;case '/terms':page=<LegalPage kind="Terms"/>;break;default:page=<NotFoundPage open={open}/>};return <NavContext.Provider value={navigate}><OnboardingProvider><Header openOnboarding={()=>open()}/>{page}<Footer/><OnboardingModal {...onboarding} close={()=>setOnboarding({open:false})}/></OnboardingProvider></NavContext.Provider>}
