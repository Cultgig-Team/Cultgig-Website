import { useEffect } from 'react';
const defaultDescription='Cultgig helps independent artists create a profile and get discovered by the people who need their work.';
export function usePageMeta(title:string,description=defaultDescription){useEffect(()=>{document.title=`${title} | Cultgig`; const set=(key:string,content:string,property=false)=>{let el=document.head.querySelector(`meta[${property?'property':'name'}="${key}"]`) as HTMLMetaElement|null;if(!el){el=document.createElement('meta');el.setAttribute(property?'property':'name',key);document.head.append(el)}el.content=content};set('description',description);set('og:title',`${title} | Cultgig`,true);set('og:description',description,true);set('og:type','website',true);set('twitter:card','summary_large_image')},[title,description])}
